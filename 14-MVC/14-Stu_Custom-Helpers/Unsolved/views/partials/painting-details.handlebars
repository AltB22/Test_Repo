<section class="card">
  <a href="/painting/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>By: {{painting.artist}}</p>
      <p>Exhibition open until: {{</p>
    </div>
  </a>
</section>